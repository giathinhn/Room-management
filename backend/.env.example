# Database
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/meeting_room_db

# JWT
JWT_SECRET=your-super-secret-key-change-in-production
JWT_ACCESS_EXPIRY=15m
JWT_REFRESH_EXPIRY=7d

# Server
PORT=5000
NODE_ENV=development

# Email (Plan 08) — Leave blank to disable email notifications
# For testing (free, no real emails): https://ethereal.email/create
# For staging:                        https://mailtrap.io/
# For production:                     smtp.gmail.com (use App Password, not account password)
SMTP_HOST=smtp.ethereal.email
SMTP_PORT=587
SMTP_USER=your-ethereal-user@ethereal.email
SMTP_PASS=your-ethereal-password
EMAIL_FROM="Meeting Room System <noreply@company.com>"

# AI Chatbot (Google Gemini) — Plan 16
# Lấy API key miễn phí tại: https://aistudio.google.com/apikey
# Để trống để tắt tính năng AI (hệ thống vẫn chạy bình thường)
GEMINI_API_KEY=your-gemini-api-key-here
GEMINI_MODEL=gemini-2.5-flash
